# 🌤️ Sky & Cloud System

## Overview

This project uses **[sky-cloud-3d](https://github.com/xiaxiangfeng/sky-cloud-3d)** - A physically-based sky and volumetric cloud rendering system for Three.js.

### Features

- 🎨 **Realistic Sky Gradient**: Dynamic sky colors simulating atmospheric scattering
- ☁️ **Volumetric Clouds**: Ray marching technique for realistic 3D clouds
- 🌅 **Dynamic Sun**: Sun position affects sky and cloud lighting
- 💨 **Wind Animation**: Clouds move with configurable wind speed
- 🔄 **Real-time Rendering**: GLSL shaders for efficient rendering

### Technical Stack

- **Engine**: Three.js v0.181.0
- **Shaders**: Custom GLSL (vertex + fragment)
- **Technique**: Perlin Noise + Ray Marching + Volume Rendering

---

## 🎛️ 完整參數配置

### 1️⃣ **App.vue 應用層參數**

位置：`src/App.vue` 第 345-355 行

```javascript
const params = {
  // === 雲層外觀 ===
  cloudCoverage: 0.45,        // 雲層覆蓋率 (範圍: 0.0-1.0)
                              // 0.0 = 無雲，1.0 = 完全覆蓋
                              // 建議值: 0.3-0.7
  
  cloudHeight: 600.0,         // 雲層高度 (單位: 像素)
                              // 雲層距離相機的垂直距離
                              // 建議值: 400-1000
  
  cloudThickness: 45.0,       // 雲層厚度 (單位: 像素)
                              // 雲層的垂直厚度
                              // 建議值: 30-100
  
  cloudAbsorption: 1.030725,  // 光線吸收率 (範圍: 0.5-2.0)
                              // 數值越高，雲越濃密暗沉
                              // 數值越低，雲越透明明亮
                              // 建議值: 0.8-1.3
  
  // === 動畫效果 ===
  windSpeedX: 5.0,            // X 軸風速 (單位: 像素/秒)
                              // 雲層在 X 軸的移動速度
                              // 建議值: 0-20
  
  windSpeedZ: 3.0,            // Z 軸風速 (單位: 像素/秒)
                              // 雲層在 Z 軸的移動速度
                              // 建議值: 0-20
  
  sunSpeed: 0.035,            // 太陽移動速度 (範圍: 0.001-0.1)
                              // 太陽在天空中的運行速度
                              // 建議值: 0.01-0.05
  
  // === 光照與渲染 ===
  exposure: 1.5,              // 整體曝光度 (範圍: 0.1-3.0)
                              // 控制整體畫面亮度
                              // 建議值: 1.0-2.5
  
  maxCloudDistance: 10000.0   // 雲層可見距離 (單位: 像素)
                              // 超過此距離的雲會淡出
                              // 建議值: 5000-20000
}
```

### 2️⃣ **渲染器設定**

位置：`src/App.vue` 第 398-399 行

```javascript
// 色調映射模式
renderer.toneMapping = THREE.LinearToneMapping
// 可選值：
// - THREE.NoToneMapping (不處理)
// - THREE.LinearToneMapping (線性，保留原始顏色)
// - THREE.ReinhardToneMapping (柔和壓縮)
// - THREE.CinematicToneMapping (電影感)
// - THREE.ACESFilmicToneMapping (電影膠片)

// 渲染器曝光度
renderer.toneMappingExposure = 2.0
// 範圍: 0.1-5.0
// 配合 params.exposure 共同控制亮度
```

---

## 🎨 顏色參數配置

### 3️⃣ **天空層顏色** 

位置：`src/utils/SkyCloudMesh.min.js`

#### 主天空漸層
```glsl
// 天空基礎顏色 (zenith - 天頂)
vec3(0.65, 0.88, 0.98)
// RGB 值: R=165, G=224, B=250
// 說明: 明亮的天藍色，天空的主色調

// 靠近太陽的顏色
vec3(1.0, 0.98, 0.85)
// RGB 值: R=255, G=250, B=217
// 說明: 淺黃白色，太陽周圍的光暈

// 地平線顏色 (horizon)
vec3(0.88, 0.94, 0.99)
// RGB 值: R=224, G=240, B=252
// 說明: 非常淺的藍白色，地平線處
```

#### 最終混合顏色
```glsl
vec3(0.92, 0.95, 0.98)
// RGB 值: R=235, G=242, B=250
// 說明: 用於地平線的額外混合，營造柔和過渡
// 混合強度: 0.2 (20%)
```

### 4️⃣ **雲朵層顏色**

位置：`src/utils/SkyCloudMesh.min.js`

#### 雲層基礎顏色
```glsl
// 雲的暗部/陰影顏色
vec3(0.75, 0.82, 0.88)
// RGB 值: R=191, G=209, B=224
// 說明: 淺藍灰色，雲層未受陽光直射的部分

// 雲的亮部基礎
vec3(1.0, 1.0, 1.0)
// RGB 值: R=255, G=255, B=255
// 說明: 純白色，雲層受陽光直射的部分

// 雲受陽光照射的邊緣顏色
vec3(1.0, 0.8, 0.5)
// RGB 值: R=255, G=204, B=128
// 說明: 暖黃色，太陽光在雲邊緣的散射效果
```

#### 雲層光照顏色
```glsl
// 太陽光顏色
vec3(1.0, 0.95, 0.8)
// RGB 值: R=255, G=242, B=204
// 說明: 淺黃白色，模擬陽光的暖色調

// 天空散射光顏色
vec3(0.7, 0.82, 0.92)
// RGB 值: R=178, G=209, B=235
// 說明: 天藍色，模擬天空對雲層的環境光照
```

### 5️⃣ **太陽顏色**

位置：`src/utils/SkyCloudMesh.min.js`

```glsl
// 太陽光暈顏色
vec3(1.0, 0.7, 0.3)
// RGB 值: R=255, G=178, B=77
// 說明: 橘黃色，太陽本體的發光顏色
// 強度: * 3.0 (會乘以 3 倍亮度)
```

---

## ☁️ 雲朵邊緣光照系統詳解

### 🌟 **雲朵受太陽光影響機制**

雲朵的邊緣顏色會根據太陽位置動態改變，這是透過多層次的顏色混合實現的。

#### **階段 1: 太陽貢獻度計算**

位置：`src/utils/SkyCloudMesh.min.js` - render_clouds 函數

```glsl
float sunContribution = pow(max(0.0, dot(rayDirection, uSunDirection)), 2.0);
```

**說明：**
- 計算視線方向與太陽方向的夾角
- 數值範圍：`0.0`（背向太陽）到 `1.0`（正對太陽）
- `pow(..., 2.0)` 讓光照效果更集中、更自然
- 這個值決定了雲朵邊緣有多少太陽光的影響

#### **階段 2: 邊緣顏色動態混合** ⭐ 核心機制

```glsl
vec3 edgeColor = mix(vec3(1.0), vec3(1.0, 0.8, 0.5), sunContribution);
```

**兩種邊緣顏色：**

| 條件 | sunContribution | edgeColor | RGB | 顏色 | 說明 |
|------|----------------|-----------|-----|------|------|
| **背向太陽** | 0.0 | `vec3(1.0, 1.0, 1.0)` | 255, 255, 255 | 純白色 `#FFFFFF` | 雲朵陰影面或側面 |
| **面向太陽** | 1.0 | `vec3(1.0, 0.8, 0.5)` | 255, 204, 128 | 暖黃色 `#FFCC80` | 被陽光照射的雲邊緣 |

**視覺效果：**
- 面向太陽的雲邊緣會出現金黃色光暈 🌅
- 背向太陽的雲保持純白色或淺灰色 ☁️
- 產生立體感和真實的光照效果

#### **階段 3: 雲朵基礎顏色混合**

```glsl
vec3 cloudColor = mix(
    vec3(0.75, 0.82, 0.88),  // 雲的暗部（陰影區）- 淺藍灰
    edgeColor,                 // 雲的亮部（受太陽影響）
    cloudLight * lightFactor   // 混合比例：光照強度
);
```

**混合邏輯：**
1. **暗部顏色** `vec3(0.75, 0.82, 0.88)` - 淺藍灰色
   - 雲層內部、陰影區域
   - RGB: 191, 209, 224 (`#BFD1E0`)

2. **亮部顏色** `edgeColor` - 受太陽影響（白色或暖黃）
   - 雲層表面、受光區域
   - 根據太陽位置動態變化

3. **混合權重** `cloudLight * lightFactor`
   - `cloudLight`: 透過光線追蹤計算的光照強度
   - `lightFactor`: 高度因子 `exp(h) / 1.75`
   - 越接近雲頂、光照越強，暖色調越明顯

#### **階段 4: 最終太陽與天空光混合**

```glsl
vec3 sunColor = vec3(1.0, 0.95, 0.8);   // 太陽散射光 - 淺黃白
vec3 skyColor = vec3(0.7, 0.82, 0.92);  // 天空散射光 - 天藍

C = C * mix(
    skyColor,           // 基礎天空光顏色
    sunColor,           // 太陽光顏色
    0.5 * pow(max(0.0, dot(rayDirection, uSunDirection)), 2.0)  // 混合比例
);
```

**說明：**
- 雲朵顏色最後還會乘以環境光顏色
- 靠近太陽方向：更多暖色調（淺黃白）
- 遠離太陽方向：更多冷色調（天藍）
- `0.5 *` 確保混合不會過度強烈

---

### 📊 **太陽光影響雲朵邊緣的完整參數表**

| 參數名稱 | 位置 | 當前值 | RGB | Hex | 作用 |
|---------|------|--------|-----|-----|------|
| **edgeColor (背光)** | shader | `vec3(1.0, 1.0, 1.0)` | 255, 255, 255 | `#FFFFFF` | 雲的陰影面邊緣 |
| **edgeColor (陽光)** | shader | `vec3(1.0, 0.8, 0.5)` | 255, 204, 128 | `#FFCC80` | 雲的陽光面邊緣（金黃光暈）|
| **sunColor** | shader | `vec3(1.0, 0.95, 0.8)` | 255, 242, 204 | `#FFF2CC` | 太陽散射光（環境光）|
| **skyColor** | shader | `vec3(0.7, 0.82, 0.92)` | 178, 209, 235 | `#B2D1EB` | 天空散射光（環境光）|
| **雲暗部** | shader | `vec3(0.75, 0.82, 0.88)` | 191, 209, 224 | `#BFD1E0` | 雲層陰影區基礎色 |

---

### 🎨 **自訂雲朵邊緣顏色範例**

#### 範例 1: 移除金黃光暈，雲保持純白
```glsl
// 將陽光面的暖色改為純白
vec3 edgeColor = mix(vec3(1.0), vec3(1.0, 1.0, 1.0), sunContribution);
```

#### 範例 2: 增強金黃光暈效果
```glsl
// 提高暖色飽和度
vec3 edgeColor = mix(vec3(1.0), vec3(1.0, 0.7, 0.3), sunContribution);
// RGB: 255, 178, 77 (#FFB24D) - 更深的橘黃色
```

#### 範例 3: 日落效果 - 粉橘色雲邊
```glsl
// 日落時分的暖橘色
vec3 edgeColor = mix(vec3(1.0, 0.9, 0.85), vec3(1.0, 0.6, 0.4), sunContribution);
// 背光: 淺粉白 (#FFE6D9)
// 陽光: 暖橘色 (#FF9966)
```

#### 範例 4: 冷色調雲朵（陰天效果）
```glsl
// 完全移除暖色調
vec3 edgeColor = vec3(0.9, 0.92, 0.95);  // 固定為冷灰藍
// RGB: 230, 235, 242 (#E6EBF2)
```

---

### 💡 **視覺效果說明**

**當太陽在不同位置時的雲朵外觀：**

1. **太陽在雲朵後方**（逆光）
   - 邊緣出現明亮的金黃色光暈 ✨
   - 雲層中心較暗（背光）
   - `sunContribution ≈ 1.0` → 使用暖黃色 edgeColor

2. **太陽在側面**（側光）
   - 部分邊緣有淡金黃色
   - 立體感最強
   - `sunContribution ≈ 0.5` → 混合白色和暖黃色

3. **太陽在觀察者後方**（順光）
   - 雲朵整體明亮
   - 邊緣接近純白色
   - `sunContribution ≈ 0.0` → 使用純白色 edgeColor

---

## 🏔️ 地面層配置

### 6️⃣ **地面區域**

位置：`src/App.vue` 第 465-473 行

```css
.bottom-white {
  position: fixed;
  bottom: 0;
  left: 0;
  width: 100%;
  height: 50vh;           /* 佔畫面下方 50% 高度 */
  background: #FFFFFF;    /* 純白色 */
  z-index: 0;
}
```

**可調整屬性：**
- `height`: 地面高度（建議值: 30vh-70vh）
- `background`: 地面顏色（任何 CSS 顏色值）
  - 純白: `#FFFFFF`
  - 淺灰: `#F5F5F5`
  - 淺藍: `#E3F2FD`

---

## 🎬 相機與控制

### 7️⃣ **相機設定**

位置：`src/App.vue` 第 377-382 行

```javascript
camera = new THREE.PerspectiveCamera(
  60,                    // FOV 視野角度 (建議: 50-80)
  aspect,                // 螢幕長寬比 (自動計算)
  0.1,                   // 近裁剪面
  30000                  // 遠裁剪面
)
camera.position.set(0, 100, 400)  // 相機位置 (x, y, z)
camera.lookAt(0, 100, 0)          // 相機朝向點
```

### 8️⃣ **OrbitControls 控制器**

位置：`src/App.vue` 第 391-398 行

```javascript
controls.enableDamping = true         // 啟用阻尼 (慣性)
controls.dampingFactor = 0.05         // 阻尼係數
controls.screenSpacePanning = false   // 禁用平移
controls.minDistance = 10             // 最小縮放距離
controls.maxDistance = 2000           // 最大縮放距離
controls.maxPolarAngle = Math.PI * 0.48  // 最大俯仰角 (限制不看太下面)
controls.minPolarAngle = 0.1          // 最小俯仰角 (限制不完全平視)
```

---

## 🚀 快速調整指南

### 場景 1: 讓雲更白更亮
```javascript
// App.vue
cloudAbsorption: 0.8,  // 降低吸收率

// SkyCloudMesh.min.js
vec3(0.88, 0.92, 0.95)  // 提高雲暗部顏色
```

### 場景 2: 讓雲更少、天空更多
```javascript
// App.vue
cloudCoverage: 0.25,   // 降低覆蓋率
```

### 場景 3: 讓天空更藍
```glsl
// SkyCloudMesh.min.js - 主天空顏色
vec3(0.5, 0.85, 1.0)   // 更深的藍色
```

### 場景 4: 加速雲層移動
```javascript
// App.vue
windSpeedX: 15.0,
windSpeedZ: 10.0,
```

### 場景 5: 白天 → 日落效果
```glsl
// SkyCloudMesh.min.js - 天空顏色
vec3(1.0, 0.6, 0.3)    // 橘紅色天空
vec3(1.0, 0.8, 0.5)    // 地平線暖色

// App.vue
sunSpeed: 0.1,         // 加快太陽移動
```

---

## 📊 效能優化建議

### 高效能設定 (適合低階裝置)
```javascript
maxCloudDistance: 5000,    // 降低渲染距離
cloudThickness: 30,        // 減少雲層厚度
renderer.setPixelRatio(1)  // 固定像素比為 1
```

### 高畫質設定 (適合高階裝置)
```javascript
maxCloudDistance: 15000,   // 提高渲染距離
cloudThickness: 80,        // 增加雲層厚度
renderer.setPixelRatio(window.devicePixelRatio)  // 使用裝置原生解析度
```

---

## 🔍 技術細節

### Shader 渲染流程
1. **Ray Marching**: 從相機發射光線，在雲層區域進行步進採樣
2. **Perlin Noise**: 使用 Perlin 噪聲紋理生成雲層密度分布
3. **Fractional Brownian Motion (FBM)**: 疊加多層 Perlin 噪聲，產生細節
4. **Light Scattering**: 計算陽光在雲層中的散射和吸收
5. **Alpha Blending**: 將雲層與天空背景進行透明混合

### 雲層密度計算
```glsl
density = fbm(position + windOffset) 
        * smoothstep(coverage)
        * heightAttenuation
```

### 光照計算
```glsl
cloudLight = exp(-absorption * density) * sunDirection
```

---

## 📝 修改檔案位置總覽

| 參數類型 | 檔案路徑 | 行數 | 說明 |
|---------|---------|------|------|
| 雲層參數 | `src/App.vue` | 345-355 | JavaScript 物件 |
| 渲染器設定 | `src/App.vue` | 398-399 | Three.js 設定 |
| 天空顏色 | `src/utils/SkyCloudMesh.min.js` | 1 | GLSL Shader |
| 雲朵顏色 | `src/utils/SkyCloudMesh.min.js` | 1 | GLSL Shader |
| 地面樣式 | `src/App.vue` | 465-473 | CSS 樣式 |
| 相機控制 | `src/App.vue` | 377-398 | Three.js Camera |

---

## 🐛 常見問題

### Q: 天空顏色改了但沒有效果？
A: 確認沒有設定 `scene.background`，讓 shader 完全控制顏色。

### Q: 雲層太暗？
A: 
1. 提高 `renderer.toneMappingExposure`
2. 降低 `cloudAbsorption`
3. 修改 shader 中的雲層暗部顏色

### Q: 看不到雲？
A: 
1. 檢查 `cloudCoverage` 是否 > 0
2. 確認 `cloudHeight` 在相機可見範圍內
3. 查看 `maxCloudDistance` 是否夠大

### Q: 雲移動太快/太慢？
A: 調整 `windSpeedX` 和 `windSpeedZ` 參數

---

## 📚 參考資源

- **原始專案**: [xiaxiangfeng/sky-cloud-3d](https://github.com/xiaxiangfeng/sky-cloud-3d)
- **Three.js 文件**: [threejs.org](https://threejs.org/)
- **線上範例**: [Demo](https://xiaxiangfeng.github.io/sky-cloud-3d/threejs-sky-clouds.html)

---

## 📄 授權

本天空系統基於 **sky-cloud-3d** 專案，僅供非商業用途。

---

**最後更新**: 2025-11-06  
**維護者**: Open Source Contributors
